<task-notification>
<task-id>a67d08ae38799c81d</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-travel-app/3c1f314f-5bc6-4f63-b253-d784d069272c/tasks/a67d08ae38799c81d.output</output-file>
<status>completed</status>
<summary>Agent "/code-review 314 high" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>The `ReportFindings` tool is not available in this session (searched by name and keyword; only unrelated deferred tools matched), so I'm reporting inline.

I reviewed the full commit `a1b5034` (`git diff main...HEAD`, working tree clean): new `lib/checkInPolicy.ts`, `checkInLegAction` in `app/actions.ts`, `app/checkin/page.tsx`, `components/ui/CheckInPanel.tsx`, the `SeatAssignment.checkedInAt` migration, the nav entry, CSS and tests. I traced the callers/helpers it depends on (`heldSeats`, `lockFlightForUpdate`, `cancelBookingAction`, `updateFlightStatusAction`, `changeBookingSeatsAction`, `passengersSeatedOnLeg`, `withRouteLabels`, `departureInOriginZone`, `safePassengerSelect`) to confirm the security and concurrency claims.

**Things I checked and found sound:** the owner-scoped query and the booking-then-leg ownership check in the action; the `heldSeats({ legId, checkedInAt: null })` idempotent update (correct — it cannot resurrect a released seat and cannot restamp); the single-flight `FOR UPDATE` lock cannot deadlock with `cancelBookingAction`'s ascending multi-flight lock, and it does serialise against it; `changeBookingSeatsAction` really does update the seat row in place, so the migration's "a check-in survives a seat change" claim holds; `TIMESTAMPTZ(3)` matches the Prisma annotation and the `SET LOCAL lock_timeout` pattern matches the 15 other migrations; window boundary maths and the DELAYED/DEPARTED interaction are correct on both boundaries.

## Findings

**1. `lib/checkInPolicy.ts:121` — medium.** `if (leg.checkedIn) return refuse('ALREADY_CHECKED_IN')` is evaluated *before* `if (leg.flightStatus === 'CANCELLED')` on line 123, contradicting the module's own precedence table in the header comment ("Flight cancelled" is listed above "Already checked in"). A traveller who checks in and whose flight is then cancelled is told "You are checked in. Bring photo identification to the airport." instead of that the flight is cancelled. In practice this is *masked* because `updateFlightStatusAction` drives such bookings to `DISRUPTED`, which outranks both — which is the same reason `FLIGHT_CANCELLED` is close to unreachable product-wide (see finding 6). Either move the check to match the documented table, or amend the table and say why the code order is the intended one.

**2. `components/ui/CheckInPanel.tsx:187` — medium.** The `&lt;dt&gt;` reads past tense (`Check-in opened`) for every reason except `NOT_YET_OPEN`, but the window `&lt;dl&gt;` is rendered unconditionally and `checkInEligibility` always returns the window regardless of reason. Cancel a booking for a flight three weeks out: the card renders "Booking cancelled" beside "**Check-in opened** — September 8 at 09:00 PDT", a past-tense label on an instant a fortnight in the future. Same for `BOOKING_DISRUPTED` and `FLIGHT_CANCELLED` on a far-off flight. This is exactly the contradiction the comment above the line says it is fixing, but the condition only covers one of the states in which the instant is still in the future. The tense should follow `opensAt &gt; now`, not `reason === 'NOT_YET_OPEN'` — or the window should not be shown at all for a booking that has no check-in.

**3. `lib/checkInPolicy.ts:157` — low.** The `DEPARTED` next step says "This flight has departed. Your other flights, if any, are **listed above**." `app/checkin/page.tsx` orders by `[{ flight: { departureDate: 'asc' } }, { sequence: 'asc' }]`, and the only legs with a past `departureDate` are the ones inside the 2-hour history window — so a departed leg is always the *first* card (or preceded only by other departed legs) and every other flight is below it. The instruction points the wrong way in every case it can be shown.

**4. `components/ui/CheckInPanel.tsx:201` — low.** `key={`${leg.legId}-${traveller.name}`}` is not unique: two travellers on one booking with the same first and last name (a parent and child sharing a name, twins) produce duplicate React keys on the same list, giving a console warning and letting React reconcile the two rows into each other — so a "Checked in" / "Not checked in" state can land on the wrong row after `router.refresh()`. `passengersSeatedOnLeg` already carries the passenger `id`; the view type drops it in the `.map` on `app/checkin/page.tsx:170`. Carry `id` through and key on it.

**5. `app/checkin/page.tsx:44` — low.** `HISTORY_HOURS = 2` bounds the query on `flight.departureDate`, which the policy is explicit is the *scheduled* instant that a delay never moves (`lib/checkInPolicy.ts` header, and the `flightStatus !== 'DELAYED'` guard on line 130). A flight delayed three hours therefore disappears from the check-in page while it is still on the ground and the customer is still at the airport — the page loses the very leg the DELAYED special case exists to keep honest. The card would only say `CLOSED`, so the loss is informational rather than blocking, but the window should be derived from a bound the delay reasoning agrees with, or the discrepancy noted where `HISTORY_HOURS` is defined.

**6. `lib/checkInPolicy.ts:123` — low (dead branch / coverage).** `FLIGHT_CANCELLED` is effectively unreachable in the running product: `updateFlightStatusAction` (`app/actions.ts:1704-1719`) is the only writer of `Flight.status = 'CANCELLED'` and always moves the affected bookings to `DISRUPTED` in the same transaction, and `BOOKING_DISRUPTED` is checked first on line 116. So the "The airline cancelled this flight. Choose a replacement from your profile…" next step and the `.checkin-state-flight-cancelled` styling in `app/globals.css` can only be reached transiently or by a direct database edit. The unit test at `__tests__/lib/checkInPolicy.test.ts:131` passes because it constructs the state directly, which no product path produces. Worth either documenting as a defensive branch or wiring the reason to something reachable.

**7. `components/ui/CheckInPanel.tsx:95` — low.** The bare `catch {}` swallows the thrown error entirely with no `console.error`. Every genuine server fault the action can raise — `"Itinerary leg not found"` from the `findFirstOrThrow` when a leg is superseded mid-request, a `P2028` transaction timeout while a concurrent `cancelBookingAction` holds the flight lock, a Prisma connection failure — is reported to the user as the same "Could not check you in. Please try again." and leaves no trace in the browser console or any log. `components/ui/titlebar.tsx` logs in the equivalent handlers (`console.error("Failed to mark as read:", err)`); this should too, so a real fault is distinguishable from a transient one during triage.</result>
<usage><subagent_tokens>118638</subagent_tokens><tool_uses>46</tool_uses><duration_ms>471832</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ae6fae560918eb38a</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-travel-app/3c1f314f-5bc6-4f63-b253-d784d069272c/tasks/ae6fae560918eb38a.output</output-file>
<status>completed</status>
<summary>Agent "Verify check-in slice on PR 314" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Role source confirmed: I read `### verifier` from `/home/scottdensmore/Developer/scottdensmore/travel-app/AGENTS.md` (lines 422-576) before any check. `/home/scottdensmore/Developer/scottdensmore/travel-app/.claude/agents/verifier.md` is a genuine pointer carrying no rules, and the on-disk section matches my injected copy — not stale. Tree was clean at start and is clean now; I changed nothing `git status` reports (verified after finishing).

## What I ran, independently, on a1b5034

Environment prep first: `npx prisma generate`, `rm -rf .next` (schema changed), Postgres and Mailpit already up and healthy, port 3000 free, Node v22.23.1.

| Check | Result |
| --- | --- |
| `npx tsc --noEmit` | clean, exit 0 |
| `npm run lint` | clean, exit 0 |
| `npm run test:unit` | **94 suites / 1041 tests passed**, exit 0 |
| `npm run test:database` | **44 suites / 317 tests passed**, exit 0 (run as a separate command, not via `npm test`) |
| `npm run build` | clean; `/checkin` listed as `ƒ` (dynamic); sanitize step reported `Removed 1 environment files from standalone output.` |
| `npx playwright test` x3 | **46 passed** (6.4m), **46 passed** (6.9m), **46 passed** (5.7m). Exit 0 each time, zero flaky, no teardown leak error |
| `npm audit --omit=dev --audit-level=high` | `found 0 vulnerabilities` |

Every number above agrees with yours. Against your `main` baseline (90 suites / 985 tests, lint clean) the unit project is +4 suites / +56 tests, which matches the four new unit-project suites. **No regression, pre-existing failure or environment problem in any check.**

`./scripts/verify-container-secrets.sh` — skipped, and your reading is right: `git diff --name-only main...HEAD` contains none of `Dockerfile`, `.dockerignore`, `next.config`, `scripts/sanitize-standalone.mjs`, `package.json` or `package-lock.json`. I flag one judgement call: the diff does change what lands in standalone output (a new route and CSS), so "standalone output" is touched in content if not in kind. I did not run it because the sanitize step ran inside my own `npm run build` and reported the expected removal, and because `.github/workflows/ci.yml` lines 78-111 do run `docker build` + `verify-container-secrets.sh` + both REJECT probe builds, and that job (`verify`) passed on this exact commit (17:56:46 → 18:05:21 UTC, 8m35s). `gh pr view 314` reports `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. I ran `npm audit` anyway even though unreachable; it costs seconds.

Playwright flakes: **I could not reproduce them in three clean runs, so the information you lost is still lost.** I reached my three-run cap. What I can add: `e2e/admin.spec.ts:80` is the `test.slow()` MFA journey and my run 1 logged `[admin.spec] TOTP window wait: 6027ms`; `e2e/disruption.spec.ts:457` is a viewport-width layout assertion. Both are timing-shaped. Combined with your 44/46, the observed rate is one failing run in six. #313 is the right disposition; do not treat three clean runs as evidence it is fixed.

## Migration, redone independently

Empty database (`vfy_ci_empty`, created from scratch): all 49 migrations applied, `_prisma_migrations` shows 49 finished. Resulting column: `checkedInAt`, `timestamp with time zone`, precision 3, nullable. All four pre-existing `SeatAssignment` indexes and four check constraints intact.

Populated database (`vfy_ci_pop`): applied all 49, ran `prisma db seed` (1417 flights), then built a real booking chain myself and inserted **200 `SeatAssignment` rows** across 4 legs and 50 passengers, every fourth released, and stamped 64 of the held ones. I built rows rather than cloning your dev database because `travel_app` had only 3 seat assignments (Playwright's in-flight fixtures) when I looked.

- Reversal exactly as the migration header documents: 200 rows preserved, `md5` checksum of every other column **byte-identical** (`03af894c…`), 4 constraints and 4 indexes intact, column gone, `_prisma_migrations` row gone. The header's claim "Reversal loses which travellers had checked in, and nothing else" is accurate.
- Re-apply against those 200 rows: succeeded, checksum still identical, `non_null_checkedInAt = 0`, `timestamp with time zone(3) null=YES`.
- **I exercised `SET LOCAL lock_timeout = '3s'` in both directions, which I do not think you did.** Holding `ACCESS EXCLUSIVE` on `SeatAssignment` from another session, `migrate deploy` failed in ~3s (5s wall including Prisma startup) with `SQLSTATE 55P03: canceling statement due to lock timeout`, wrapped as Prisma `P3018`. That proves two things: the timeout is real, and the `SET LOCAL` is genuinely inside a transaction — outside one Postgres downgrades it to a warning and does nothing, and the ALTER would have blocked the full 25s. The failure rolled back whole (`column_present = 0`), and `npx prisma migrate resolve --rolled-back 20260818120000_record_traveller_check_in` followed by `migrate deploy` recovered cleanly. The Gotchas recovery procedure works verbatim on this migration. 16 other migrations use the same `SET LOCAL lock_timeout` pattern.
- Partial unique index, both directions, with the new column present: a second **held** row on the same `(flightId, seatNumber)` was rejected by `SeatAssignment_flightId_seatNumber_held_key`; a **released** row on the same pair *carrying a `checkedInAt` stamp* was accepted and read back as the same instant. That is the "checked in, then cancelled" case the header says it deliberately permits, confirmed rather than assumed.

**The mixed TIMESTAMPTZ / TIMESTAMP pair is not a hazard here, and `checkedInAt` is the correct one of the two.** `Flight.departureDate` is already `timestamp with time zone` (per #84), and that is the column `checkedInAt` is compared against — so `releasedAt` is the outlier, not the new column. The session `TimeZone` on this server is `UTC`, making any implicit cast a no-op, and I found no product raw SQL comparing either column against an instant (the only `SET "releasedAt" = CURRENT_TIMESTAMP` is inside the trigger in `prisma/migrations/20260810050000_release_seats_without_renaming/migration.sql:156`). The prospective risk is narrow and worth naming in the follow-up issue for converting `releasedAt`: a deployment whose session `TimeZone` is not UTC, plus raw SQL comparing `releasedAt` to an instant or to `checkedInAt` directly. Nothing in this slice reaches it.

## Mutant audit

I audited from source without reproducing. **Every claim I could audit holds.** Spot-checking the reasoning behind the four rows you flagged:

- **M1/M2 are genuine boundary-exact mutants, not near-misses.** `__tests__/lib/checkInPolicy.test.ts:93` sets `departsAt = now + CHECK_IN_CLOSES_MINUTES`, so `closesAt === now` exactly; `&gt;=`→`&gt;` falls through to `OPEN`. Line 70 sets `departsAt = now + CHECK_IN_OPENS_HOURS`, so `opensAt === now`; `&lt;`→`&lt;=` yields `NOT_YET_OPEN`. Both assertions are on `reason`, which the mutation changes.
- **M5's reported count is right.** Three tests fail (lines 282, 296, 320), which is a small but real signal on reporting accuracy — the #231 error was a "two suites" that was one.
- **M7's survival story checks out.** `leaves the first stamp alone when pressed twice` returns at `app/actions.ts:771` before reaching `updateMany`, so nothing in the original suite could see `checkedInAt: null` disappear. The closing test at `__tests__/app/checkIn.database.test.ts:306` asserts `after[0].checkedInAt?.getTime()).toBe(earlier.getTime())`, which the mutant violates directly. The 11-test file matches your "1 skipped, 10 passed".
- **M13/M14 both check out.** M13 is killed by an exact `toEqual(['Ada Lovelace'])` (not `arrayContaining`). M14's kill depends on the anonymous default being re-established, and it is — `__tests__/components/titlebar.test.tsx:41` re-sets `useSession` to `{ data: null }` in `beforeEach` with a comment explaining exactly that leakage. Good.
- **M16 is a real character-for-character check.** The literal is `'…in your profile to rebook, then check in.'` and the assertion is `.toContain('rebook')`. It matches the text it is aimed at, unlike the #248 example.
- **M12 is constrained by `toEqual({ userId: 'u1' })`**, an exact-object match, not `objectContaining`. Deleting the clause yields `undefined` and fails.
- **M15 is the best mutant in the table.** Rendering the boundary from an unknown place falls back to UTC, and the assertion is the exact string `'Aug 18, 2026 at 11:00 PDT'`. That defeats the "boundary in the origin's zone" claim rather than confirming it.

**Your M24 reasoning is sound — the refactor closed a real hole, it did not hide one.** I checked observability from the source: `leg.checkedIn` is read at exactly one place, `if (leg.checkedIn) return refuse('ALREADY_CHECKED_IN')` (truthiness only), and `CheckInEligibility` has no field that carries it out — only `allowed`, `reason`, `opensAt`, `closesAt`. `legCheckInEligibility` derives `travelling`/`awaiting` from the seat rows, not from the folded value. And `app/checkin/page.tsx:179-181` computes each traveller's `checkedIn` independently from `leg.seatAssignments`, so the policy's fold is never the source of anything displayed. The instant was genuinely unobservable. Changing the field to `boolean` makes the mutant unrepresentable in the type, which is stronger than a test — that is a legitimate close, not an evasion.

**Your `NOT_TRAVELLING`-placement question: it is constrained, but by a mechanism no reader of the test would see.** I first concluded that deleting `&amp;&amp; outcome.allowed` from `lib/checkInPolicy.ts:224` would survive, then found why it does not. `prisma/migrations/20260810050000_release_seats_without_renaming/migration.sql:152-170` installs `Booking_releases_seats_when_cancelled`, an `AFTER UPDATE OF status` trigger that releases every held seat when a booking becomes `CANCELLED`. So in `__tests__/app/checkIn.database.test.ts:311`, `prisma.booking.update({ status: 'CANCELLED' })` silently releases the seat, making `travelling === 0`; without the `&amp;&amp; outcome.allowed` guard the reason flips to `NOT_TRAVELLING`, whose next step does not contain the word "cancelled", and the assertion at line 329 fails. Real kill. Two consequences worth stating: the ordering *is* defended, and the defence rests on a database trigger that neither the test nor its comment mentions — if that trigger ever changes, this assertion stops constraining the ordering and nothing will say so.

The same trigger analysis also answers whether `NOT_TRAVELLING` masking is reachable. The only two writers of `releasedAt` are that trigger (which sets `status = CANCELLED`, and `bookingStatus` is checked before the clock) and `lib/itineraryRebookingService.ts:253` (which sets `supersededAt` on the same leg, excluded by `activeItineraryLegWhere`). So a live, non-superseded leg with every seat released is unreachable through product code, consistent with the type's own doc calling it "a defect rather than a state". The `NOT_YET_OPEN`-masks-`NOT_TRAVELLING` case I was worried about is not reachable. Not a finding.

**`__tests__/styles/checkIn.test.ts` is real coverage, not theatre.** Three reasons: `app/globals.css` ships to the browser, so it is product code by the Testing Expectations criterion; the first test (`expect(css).toContain('.checkin-card {')`) is a genuine guards-the-guard against the wrong-file vacuity that would pass everything; and I checked the cascade — the only two `outline: none` rules in the file are at lines 306 and 1066 on `.fields-container select` and `.admin-card input[type="text"]:focus`, neither of which can match `.checkin-feedback`, and no rule after line 2057 touches `outline`. So the assertion is not vacuous today. Its limits, which are worth knowing rather than fixing: it pins whitespace, declaration order and the exact hex values, so a legitimate restyle breaks it for no accessibility reason; and it verifies a *string*, so a future more-specific overriding rule would nullify the behaviour while the test stayed green. Given the `__tests__/styles/responsiveDashboards.test.ts` precedent, this is the house form. Acceptable.

## Findings

**1. The flight lock is asserted nowhere, and this repo already has the pattern to assert it. (coverage gap — actionable, cheap)**

`app/actions.ts:748`, `await lockFlightForUpdate(tx, leg.flightId);`. Deleting that line leaves all 11 tests in `__tests__/app/checkIn.database.test.ts` green — nothing in the file is concurrent, and `leg.flightId` becoming unused is not a lint error. It is an unattempted mutant that survives, and it is the one line in the action whose comment makes the strongest claim ("Without it, READ COMMITTED lets a cancellation commit between the read below and the update"). You declared "nothing exercises the flight lock concurrently" as a gap, but the gap is not really concurrency — it is that *no assertion mentions the lock at all*, and two existing suites show how without any concurrency: `__tests__/lib/checkoutPaymentService.database.test.ts:575` and `__tests__/lib/itineraryRebookingService.database.test.ts:239` both `jest.mock` `@/lib/flightLock` with an `actual` passthrough and assert on `lockSpy.mock.calls`, the second even ordering it against another lock via `invocationCallOrder`. One test of the shape "locks the leg's flight before deciding" closes this.

**2. The action's superseded-leg exclusion is an unasserted claim. (coverage gap — actionable)**

`app/actions.ts:735` and `:753` both carry `...activeItineraryLegWhere`, and the comment at 736-737 states "A superseded leg lands here too. Its replacement is the one that can be checked in." No test in any project supersedes a leg and calls `checkInLegAction`, so deleting `...activeItineraryLegWhere` from both reads survives. Severity is moderate rather than high, and I want to be accurate about why: because `itineraryRebookingService` releases the old leg's seats as it supersedes them, a superseded leg reaches `legCheckInEligibility` with `travelling === 0` and `heldSeats` would stamp nothing regardless. So the realistic failure is a confusing refusal ("No traveller on this booking holds a seat on this flight. Contact us so we can look into it.") on a leg that was simply replaced, not a bad stamp. Note the contrast: the *page's* copy of this filter **is** asserted (`__tests__/app/checkInPage.test.ts:115`, `expect(where.supersededAt).toBeNull()`); only the action's two copies are not.

**3. The mutant table you handed me does not match the notes file, and four rows are unauditable. (audit finding — reporting)**

Your invocation says 26 mutants. `/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-travel-app/3c1f314f-5bc6-4f63-b253-d784d069272c/scratchpad/mutants.md` contains **22 rows** (M1-M22), and is internally inconsistent about its own count: its round-1 header says "17 mutants" above a 16-row table, and its footer says "23 mutants in total" above 22 rows. **M23, M25 and M26 appear nowhere at all** — not in the file, not in the invocation. **M24 appears only as invocation prose**, with no row giving `file:line`, the exact edit, the suite, or the outcome. Per the rule I work to, an unreported mutant is an unaudited one, silently, and #231 was exactly a count that was wrong in miniature. I audited 22 claims plus M24 from your prose; I cannot say anything about three named mutants I was never shown. Either the three exist and their rows were lost, or the count is wrong — both need correcting before this goes to review, because the difference decides whether the selection was as broad as claimed.

Related hygiene, not blocking: the round-1 line numbers are stale by 8-10 lines against the committed file (M1 says `checkInPolicy.ts:127`; the `closesAt` guard is at `:135`; M4 says `:207`, the fold is at `:217`). That is consistent with the M24 refactor adding the `checkedIn` doc comment afterwards. The "exact edit" column was specific enough that I could still locate every one, which is the column that saved this.

**4. Two assertions with headroom and no mutant reported. (coverage gap — minor, but this is the #248 shape)**

- `__tests__/app/checkInPage.test.ts:131`, `expect(take).toBeLessThanOrEqual(100)` against `MAX_LEGS = 60`. Forty of headroom; raising the ceiling to 100 leaves it green. I think this one is *defensible* — the property it defends is "bounded at all", after `/flights` shipped with no ceiling (#153) — but it is a limit with headroom, no mutant was named for it, and #248 is four guards of exactly that shape that shipped past their own tables. Say the headroom is deliberate rather than leaving it silent.
- `__tests__/lib/checkInPolicy.test.ts:216`, `expect(step.length).toBeGreaterThan(0)` across all nine reasons. `return reason;` in every case of `checkInNextStep` leaves this green; the enum-leak test at line 224 catches only three reasons, and `'OPEN'` has no underscore so it would not match `/[A-Z]{2,}_[A-Z]{2,}/` either. The exhaustiveness is really carried by TypeScript's return type, not by this assertion.

**5. The panel's intro duplicates the policy constants as prose. (coverage gap — minor)**

`components/ui/CheckInPanel.tsx:111-112` hard-codes "Check-in opens 24 hours before each flight leaves and closes an hour before it." while `CHECK_IN_OPENS_HOURS` and `CHECK_IN_CLOSES_MINUTES` live in `lib/checkInPolicy.ts:34,42`. Change either constant and the intro lies, with no test failing — every other user-visible mention of the window is derived (`checkInNextStep` interpolates both, and `__tests__/app/checkInPage.test.ts:222` asserts against the constant). This is the one place the number is a literal.

**6. React key collides for two travellers with the same name. (minor)**

`components/ui/CheckInPanel.tsx:201`, `key={`${leg.legId}-${traveller.name}`}`. Two passengers on one leg with identical first and last name — a father and son, which a real booking has — produce duplicate keys and a React warning. The passenger id is available at `app/checkin/page.tsx:180` but is not carried into `CheckInLegView`.

## Verdict on the gaps you declared

- **Nothing exercises the flight lock concurrently** — the concurrency part is acceptable to ship with the gap named; the repo has no concurrent test for any other `lockFlightForUpdate` call site either. But finding 1 is a different and stronger version of this gap, and I would not ship without it: a one-line spy assertion, the pattern already exists twice, and without it the whole serialisation argument in that comment is unguarded.
- **`CLOSED`, `DEPARTED`, `BOOKING_CANCELLED`, `BOOKING_DISRUPTED`, `NOT_TRAVELLING` never rendered in a browser** — acceptable with the gap named. All five reach the same `STATUS_LABELS[reason]` / `checkInNextStep(reason)` lookups, both exhaustively asserted at `__tests__/app/checkInPage.test.ts:227-246`, and `e2e/checkin.spec.ts` renders the `OPEN`/`NOT_YET_OPEN`/`ALREADY_CHECKED_IN` trio in a real browser. One correction to how you framed it, though: `CLOSED`, `DEPARTED`, `BOOKING_DISRUPTED` and `NOT_TRAVELLING` are also untested at the **action** level (`__tests__/app/checkIn.database.test.ts` covers `NOT_YET_OPEN`, `FLIGHT_CANCELLED`, `BOOKING_CANCELLED` only) — which is not the gap you declared. Low severity, since all of them exit through the single `if (!eligibility.allowed) return eligibility.reason` at line 772 and three representatives exercise it.
- **`:focus` and link-underline pinned only textually** — acceptable. See the styles analysis above; it is not vacuous today, and the wrong-file failure mode is guarded.

Nothing I found blocks the slice on correctness. Findings 1 and 3 I would fix before code review — finding 3 because the count discrepancy is precisely the thing my audit exists to catch and I cannot certify a selection I was not shown, and finding 1 because it is minutes of work against an established pattern. Findings 2, 4, 5 and 6 are legitimately shippable with the gap named, or as follow-up issues per the Issue Tracking rules.

Scratch artifacts: both databases I created (`vfy_ci_empty`, `vfy_ci_pop`) are dropped. `travel_app_rebooking_ui_test`, `travel_app_rebooking_ui_rehand_234_test`, `travel_app_refund_test` and `travel_app_receipt_review_test` remain and are not mine. After my three Playwright runs `travel_app` has zero bookings, zero seat assignments, zero `CI*`/`CKI-*`/`VFY-*` flights and zero `checkin-*` accounts.</result>
<usage><subagent_tokens>142552</subagent_tokens><tool_uses>81</tool_uses><duration_ms>1622433</duration_ms></usage>
</task-notification>