Look at the AGENTS.md and scrutinize the workflow using ultracode. Make sure all the subagents are correct and propose any changes

---

<task-notification>
<task-id>wdkjo1egz</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-travel-app/be5618d4-2d56-4d15-8f3c-aaaba6a29b03/tasks/wdkjo1egz.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Scrutinize AGENTS.md development workflow and sub-agent definitions for gaps, contradictions and unowned checks" completed</summary>
<result>{"synthesis":"# Agent-workflow review — proposed edits\n\n27 real problems, deduplicated. Verifier-corrected versions used throughout. Every quoted line was re-read from disk.\n\nApply in the order given inside each tier: several test edits depend on prose edits landing first.\n\n---\n\n## Tier 1 — the rulebook makes an agent do the wrong thing\n\n### 1. `AGENTS.md:39-41` claims the database Jest project fails closed on `DATABASE_IS_DISPOSABLE`. Nothing in it checks. (high)\n\n`jest.config.js:55` gives the database project `globalSetup: '&lt;rootDir&gt;/jest.database-setup.js'`, and that file's only export is `assertSerialised` (`jest.database-setup.js:20-33`), which throws only on `maxWorkers &gt; 1`. A repo-wide grep for the marker returns `.env.example`, `README.md`, `AGENTS.md`, `.github/workflows/ci.yml:15`, and `e2e/global-setup.ts:99` — nowhere else. The only enforcement is `assertDisposableDatabase()` (`e2e/global-setup.ts:127`), reached from the database project through exactly one test, `__tests__/e2e/runIsolation.database.test.ts:71`, i.e. lazily and in Jest's file order. The other 21 `*.database.test.ts` files write and `deleteMany` first.\n\n**Preferred fix (code, so the doc becomes true).** Extract the destination check into CommonJS — `scripts/assertDisposableDatabase.js` exporting `assertDisposableDatabase` (move the body of `e2e/global-setup.ts:127-160` verbatim, including `DISPOSABLE_DATABASES` and `DISPOSABLE_DATABASE_PATTERN`), have `e2e/global-setup.ts` import it, and call it in `jest.database-setup.js`:\n\n```js\nmodule.exports = async function databaseGlobalSetup(globalConfig) {\n    assertSerialised(globalConfig);\n    assertDisposableDatabase();   // #NNN: the project deletes rows; refuse a database it does not own\n};\n```\n\nDo not `require()` the `.ts` file from `globalSetup` — Jest's transform coverage of globalSetup requires is version-dependent and this must fail closed, not fail to load.\n\n**If the code fix is deferred, `AGENTS.md:39-41` must be corrected to the truth:**\n\n&gt; `.env.example` also carries `DATABASE_IS_DISPOSABLE=true`. A Playwright run deletes every booking in the database it is pointed at and refuses to start without it. The database Jest project deletes rows too but does **not** refuse: its `globalSetup` only checks the worker count (`jest.database-setup.js`), and the disposability check is reached lazily, from the one file that calls `clearPreviousRunBookings`, after the other files have already written. Never point `DATABASE_URL` at anything you would miss.\n\n**Test impact:** none. Add a focused test asserting `jest.database-setup.js` throws when `DATABASE_IS_DISPOSABLE` is unset. Mutant: delete the `assertDisposableDatabase()` call, not a character inside the message.\n\n---\n\n### 2. `docs/UI_REVIEW.md:10` asserts a capability limit that does not hold, and states no rule in its place. (high)\n\nLine 10: *\"You review. You do not fix: you have no editing tools, and that is deliberate.\"* False — `.claude/agents/ui-review.md:4` grants `Bash`, and `docs/UI_REVIEW.md:39` tells the agent to use it (`npm run dev`). `docs/VERIFIER.md:13-22` states the honest version for the sibling agent; grepping UI_REVIEW.md for `git status` / `change nothing` / `tracked` returns only line 25, which is about reading the diff. Nothing upstream supplies it: `AGENTS.md:187` bars the *verifier* by name, and `AGENTS.md:181-182` assigns fixing to the main agent rather than prohibiting investigative writes. ui-review runs immediately before the verifier certifies the tree (`AGENTS.md:138` then `:151`).\n\n`browser_evaluate` is **not** part of this. It mutates an ephemeral DOM and a database the review is *supposed* to drive (`docs/UI_REVIEW.md:40, 50-52`). The hazard is tracked source via Bash.\n\n**Replace `docs/UI_REVIEW.md:10-11` with:**\n\n&gt; You review. You do not fix, and that is a rule rather than a capability limit: you have no Edit or Write tool, but Bash can write to any file in the tree and `sed -i` is not a loophole. **Change nothing `git status` would report.** The main agent runs the verifier straight after you and it reads from disk throughout, so an edit underneath it certifies a tree that never existed at any single moment (AGENTS.md step 7). Report findings and let the main agent act on them.\n&gt;\n&gt; The line is tracked source. A dev server, `.next/`, and the screenshots this review requires are not violations, and neither is data you create by driving the app — exercising a real booking is the point.\n\n**Test impact:** safe as written (`__tests__/docs/agentInstructions.test.ts:287` still matches on \"you have no\"), which is itself finding 8. Do not drop the phrase \"you have no\" before finding 8 lands.\n\n---\n\n### 3. `docs/VERIFIER.md:60` hands over the solely-owned database project through `npm test`, which short-circuits. (high)\n\n`package.json:13` is `\"test\": \"npm run test:unit &amp;&amp; npm run test:database\"`. `AGENTS.md:74-75` says so outright. `docs/VERIFIER.md:60` is the only Jest command anywhere in `docs/`, and it backs the verifier's row‑1 ownership at `AGENTS.md:164`. `docs/VERIFIER.md:80-97` already documents the two neighbouring traps in these same scripts (the `test:unit -- &lt;path&gt;` filter trap at 90-93, the `--selectProjects database` abort at 94-97); this third one is the only member of the family missing.\n\nRealistic exposure is narrower than \"the slice ships\" — `AGENTS.md:154-157` forces a fix and rerun on any actionable finding. It is the path the workflow explicitly anticipates: a unit failure classified pre-existing and *explicitly resolved* without a code change. Then there is no rerun, the database project never runs against the final tree on any invocation for that branch, and `docs/VERIFIER.md:167` (\"an unrun check is not a passing one\") is silently unsatisfiable because the verifier does not know it was unrun.\n\n**Replace `docs/VERIFIER.md:60`:**\n\n```bash\nnpm run test:unit           # the unit project\nnpm run test:database       # the database project; --runInBand, needs Postgres\n```\n\n**and add one sentence under that fence:** \"Run those two as separate commands and report each result separately — never chained, and never via `npm test`.\"\n\n**Add to `## Environment traps that produce false results` (beside its two siblings at 90-97):**\n\n&gt; - `npm test` is `test:unit &amp;&amp; test:database`. A failing unit test means the database project never ran — you own it, so run `npm run test:database` anyway and report its result, rather than letting one red project stand in for the other's absence\n\n**Test impact:** load-bearing. `CHECKS_THE_VERIFIER_OWNS` entry `['the full unit and database projects', /database project/]` (`:116`) matches from `## The checks` down; the replacement comment preserves the literal. Do not reword it to \"database tests\".\n\n---\n\n### 4. Step 7 requires a `main` baseline no step produces, and the cost split forbids measuring it. (high)\n\n`AGENTS.md:228-231` — *\"**Say what is failing on `main`** when invoking it, or the classification is a guess dressed as a result\"* — against `AGENTS.md:164-166`, which assigns the build, the full Playwright run and the database project to the verifier, and `:171-172`, which forbids duplicating them. Grepping `AGENTS.md`, `docs/VERIFIER.md`, `docs/UI_REVIEW.md`: every hit *consumes* a baseline (`docs/VERIFIER.md:114`, `:164`); nothing produces one. Steps 1 and 2 are the only moments the agent stands on `main` and say nothing.\n\n**Append to `AGENTS.md:112-115` (step 2), after \"create the branch from the latest appropriate `main` state\":**\n\n&gt; While still on `main`, run `npm run lint` and `npm run test:unit 2&gt;&amp;1 | grep -E '^Tests:'` and keep the result — that record is the baseline step 7 requires you to hand the verifier. You do not own the build, the database project or the full Playwright run, so you will have no baseline for those: say so in the invocation ra
... (truncated 30963 chars, full result in /tmp/claude-1000/-home-scottdensmore-Developer-scottdensmore-travel-app/be5618d4-2d56-4d15-8f3c-aaaba6a29b03/tasks/wdkjo1egz.output)</result>
<diagnostics>Per-agent results: /home/scottdensmore/.claude/projects/-home-scottdensmore-Developer-scottdensmore-travel-app/4225593f-009f-4a36-b708-e9f8ae313cf3/subagents/workflows/wf_e6577565-31f/journal.jsonl — one {"type":"result",...} line per completed agent with its full return value.
If the result above is empty or unexpected, Read this file BEFORE diagnosing — do not assume agents returned non-empty results.
To re-run with edited post-processing: Workflow({scriptPath: '/home/scottdensmore/.claude/projects/-home-scottdensmore-Developer-scottdensmore-travel-app/4225593f-009f-4a36-b708-e9f8ae313cf3/workflows/scripts/scrutinize-agents-workflow-wf_e6577565-31f.js', resumeFromRunId: 'wf_e6577565-31f'}) — agents whose (prompt, opts) are unchanged replay from cache.</diagnostics>
<usage><agent_count>13</agent_count><agents_done>13</agents_done><agents_error>0</agents_error><agents_skipped>0</agents_skipped><agents_empty_result>0</agents_empty_result><subagent_tokens>749205</subagent_tokens><tool_uses>169</tool_uses><duration_ms>806869</duration_ms></usage>
</task-notification>

---

Ok take these slices