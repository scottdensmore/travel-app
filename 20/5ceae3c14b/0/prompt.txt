In the travel-app Next.js project, add proper NextAuth/TypeScript type augmentation so the codebase stops using `as any` to read `session.user.id` and `session.user.role`.

Currently `@typescript-eslint/no-explicit-any` is set to "warn" in .eslintrc.json because of ~17 deliberate `any` casts, most of them around the NextAuth session/JWT. Files with these casts include: lib/auth.ts (jwt/session callbacks and the credentials object), app/actions.ts (session.user as any).id/.role in several actions), app/profile/page.tsx, app/admin/page.tsx, components/ui/titlebar.tsx, and a couple of chart components.

Tasks:
1. Create a types/next-auth.d.ts that augments `next-auth` (Session.user with id + role) and `next-auth/jwt` (JWT with id + role), importing the Prisma `Role` enum for the role type.
2. Remove the `as any` casts in lib/auth.ts callbacks, app/actions.ts, profile/admin pages, and titlebar — relying on the augmented types instead.
3. For any remaining legitimate `any` (e.g. recharts tooltip formatters in chart components), type them properly or leave a targeted eslint-disable with a comment.
4. Once the `any` casts are gone, re-tighten `@typescript-eslint/no-explicit-any` back to "error" in .eslintrc.json.
5. Verify: `npm run lint` (0 errors, ideally 0 no-explicit-any warnings), `npx tsc --noEmit` (0 errors), `npm test` (all green).

Follow the repo's workflow: feature branch, TDD where applicable, code review before PR, then open and squash-merge the PR against scottdensmore/travel-app (it's a fork — base the PR on scottdensmore/travel-app, not the upstream).