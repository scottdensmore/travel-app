# Automatic Codex Review Smoke Test

This temporary file creates a ready-for-review pull request that verifies the
repository's configured automatic Codex review starts without a manual trigger.

The pull request is not intended to merge. Remove its branch after the review
signal has been observed.
